# In-session canon Add/Remove

2026-09-09 16:39:10 Australia/Brisbane.

The player reported that UI testing looked good and requested committing the
remaining implementation after the preceding documentation-only commit.

## Changes

- Add forms for characters, locations, facts and items, with suggested permanent
  IDs, complete field coverage, searchable references and detached drafts.
- Core validates the complete creation request, including ID collisions and stale
  references, before applying it and saving once. No turn or pack content is changed.
- Typed removal plans show precise cross-entity consequences, protect the player,
  and refuse replaced targets or require review of changed dependencies.
- Desktop actions preserve the story draft and narration position, refresh details
  and links, and require reopening after a possible partial save failure.
- Record the player's manual-review report in the task, design, README and backlog.

## Validation and scope

The final implementation previously passed `dotnet build StoryWeaver.sln --no-restore
-c Release` with zero warnings and errors. Application code has not changed since
that build. Whitespace validation passes. The player reported successful UI review;
individual backend race/failure scenarios were not separately confirmed. No runtime
tests or model calls were run by the agent.

No new projects, dependencies or save-format changes. Player discovery, offstage
departure extraction and their evals remain design work, not part of this implementation.
