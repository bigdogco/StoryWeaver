# Window placement persistence

Main and Library independently remember normal size, position and maximized state.
Restore constrains placement to a connected screen. Placement files are separate
from view preferences. Release build passed with zero warnings and errors; monitor
and window-state manual checks are tracked in FUTURE_WORK.
